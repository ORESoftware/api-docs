{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/oresoftware/api-docs/raw/main/json-schema/route-map.schema.json",
  "title": "Ores API route map",
  "description": "Language-neutral RPC/HTTP contract: an object whose keys name operations and whose values are routes. This is the interchange format. Languages may *author* the same keys as method annotations, param types, return types, function types, or a combination; they serialize to this map. Close to OpenAPI operationId→path, Connect method→POST path, and OpenRPC method names — not a perfect instance of any one of those.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_version",
    "service",
    "map"
  ],
  "properties": {
    "schema_version": {
      "const": "1.0.0"
    },
    "service": {
      "type": "string",
      "minLength": 1,
      "pattern": "^[a-z0-9][a-z0-9._-]*$",
      "description": "Process or crate that owns these routes (pmap-api-server, pmap-web-server)."
    },
    "title": {
      "type": "string",
      "minLength": 1
    },
    "version": {
      "type": "string",
      "minLength": 1
    },
    "description": {
      "type": "string"
    },
    "map": {
      "type": "object",
      "minProperties": 1,
      "propertyNames": {
        "type": "string",
        "minLength": 1,
        "pattern": "^[A-Za-z][A-Za-z0-9_]*$",
        "description": "Operation key. PascalCase implies Connect JSON unary POST /{service}/{Key}."
      },
      "additionalProperties": {
        "$ref": "#/$defs/routeValue"
      }
    },
    "files": {
      "description": "Optional extra 1:1 key→source file. Not required. Leptos/Dioxus can use Next-style path correspondence; HTTP still allows many verbs in one file.",
      "type": "object",
      "additionalProperties": {
        "type": "string",
        "minLength": 1
      }
    }
  },
  "$defs": {
    "httpMethod": {
      "type": "string",
      "enum": [
        "GET",
        "POST",
        "PUT",
        "PATCH",
        "DELETE",
        "HEAD",
        "OPTIONS"
      ]
    },
    "httpPath": {
      "type": "string",
      "minLength": 1,
      "pattern": "^/[^\\s]*$"
    },
    "routeBinding": {
      "type": "object",
      "additionalProperties": false,
      "properties": {
        "annotation": {
          "type": "string",
          "minLength": 1
        },
        "param_types": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          }
        },
        "return_type": {
          "type": "string",
          "minLength": 1
        },
        "function_type": {
          "type": "string",
          "minLength": 1
        },
        "file": {
          "type": "string",
          "minLength": 1
        },
        "symbol": {
          "type": "string",
          "minLength": 1
        }
      },
      "anyOf": [
        {
          "required": [
            "annotation"
          ]
        },
        {
          "required": [
            "param_types"
          ]
        },
        {
          "required": [
            "return_type"
          ]
        },
        {
          "required": [
            "function_type"
          ]
        }
      ]
    },
    "routeObject": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "path"
      ],
      "properties": {
        "path": {
          "$ref": "#/$defs/httpPath"
        },
        "methods": {
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/httpMethod"
          }
        },
        "summary": {
          "type": "string"
        },
        "binding": {
          "$ref": "#/$defs/routeBinding"
        },
        "path_params": {
          "description": "JSON Schema object whose properties are RFC 6570 / OpenAPI path placeholders. Property names must equal the {vars} in path.",
          "type": "object",
          "minProperties": 1
        },
        "query_schema": {
          "description": "JSON Schema (2020-12) object for typed query-string parameters. Property names are query keys.",
          "type": "object"
        },
        "request_schema": {
          "description": "JSON Schema (2020-12) object for the JSON request body / RPC payload. Param types in source should match this.",
          "type": "object"
        },
        "response_schema": {
          "description": "JSON Schema (2020-12) object for the success payload. Return types in source should match this.",
          "type": "object"
        },
        "error_schema": {
          "description": "JSON Schema (2020-12) object for a documented error payload.",
          "type": "object"
        },
        "alias_of": {
          "description": "Another map key this route aliases (REST alias of a Connect method).",
          "type": "string",
          "minLength": 1,
          "pattern": "^[A-Za-z][A-Za-z0-9_]*$"
        },
        "transports": {
          "description": "Wires that carry this key. The JSON call/receipt frame is the same on each; HTTP uses path+method, WebSocket and TCP send the frame, NATS publishes the same JSON. Omit to infer (HTTP, or WebSocket when path is /ws).",
          "type": "array",
          "minItems": 1,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/transport"
          }
        },
        "tcp_framing": {
          "description": "TCP byte framing when transports includes tcp. NDJSON is one JSON object per line (same as ai-agent-bridge). length-prefixed is a 4-byte big-endian length then that many JSON bytes. Not a git dependency on that service.",
          "type": "string",
          "enum": [
            "ndjson",
            "length-prefixed"
          ]
        },
        "delivery": {
          "description": "How a mutation reaches the owner. direct is an RPC on the declared transports. opto_sync_queued is checked against opto-sync's real constraints at map-parse time (mutating methods, SQL-safe table, record payload on upsert, no body on delete) so a mismatch fails here instead of a live mutation. This crate does not depend on opto-sync.",
          "type": "string",
          "enum": [
            "direct",
            "opto_sync_queued"
          ]
        },
        "opto_sync": {
          "description": "Required when delivery is opto_sync_queued. table is an opto-sync scope id, not a crate import.",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "table",
            "operation"
          ],
          "properties": {
            "table": {
              "type": "string",
              "pattern": "^[A-Za-z_][A-Za-z0-9_]{0,62}$"
            },
            "operation": {
              "enum": [
                "upsert",
                "delete"
              ]
            }
          }
        },
        "header_schema": {
          "description": "Typed request headers used only for validation and serialization. They never participate in operation selection, which is method + path only.",
          "type": "object"
        }
      }
    },
    "transport": {
      "type": "string",
      "enum": [
        "http",
        "tcp",
        "websocket",
        "nats"
      ]
    },
    "routeValue": {
      "oneOf": [
        {
          "$ref": "#/$defs/httpPath"
        },
        {
          "$ref": "#/$defs/routeObject"
        }
      ]
    }
  }
}
