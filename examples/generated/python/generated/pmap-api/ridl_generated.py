"""
Generated by ridl from pmap-api.route-map.json. DO NOT EDIT.
Edit the route map and re-run `ridl generate`; a drift check fails the build if this file and the map disagree.

service: pmap-api-server
version: 0.2.0
"""

from __future__ import annotations

import json
from dataclasses import dataclass, field
from typing import Any, Literal, NewType, Protocol

SERVICE = "pmap-api-server"
VERSION = "0.2.0"

MatterId = NewType("MatterId", str)
"""Identifies one matter. Distinct from a bare string so a client cannot pass an arbitrary id."""

Entitlement = Literal["free_mvp", "paid_advisor"]
ENTITLEMENT_VALUES: tuple[Entitlement, ...] = ("free_mvp", "paid_advisor",)

NodeKind = Literal["disclaimer", "interview", "branch", "form_guide", "task", "counsel", "document_pack", "terminal"]
NODE_KIND_VALUES: tuple[NodeKind, ...] = ("disclaimer", "interview", "branch", "form_guide", "task", "counsel", "document_pack", "terminal",)

CounselProvider = Literal["grok", "chatgpt", "claude", "gemini"]
COUNSEL_PROVIDER_VALUES: tuple[CounselProvider, ...] = ("grok", "chatgpt", "claude", "gemini",)

VerdictSource = Literal["heuristic", "grok", "chatgpt", "claude", "gemini"]
VERDICT_SOURCE_VALUES: tuple[VerdictSource, ...] = ("heuristic", "grok", "chatgpt", "claude", "gemini",)

AskCounselScope = Literal["question", "options", "review"]
"""Which brief the server builds. Defaults to `question`."""
ASK_COUNSEL_SCOPE_VALUES: tuple[AskCounselScope, ...] = ("question", "options", "review",)

ReviewDocument = Literal["prenup", "setup", "disclosure", "worksheet"]
"""Which generated document AskCounsel critiques when scope is `review`."""
REVIEW_DOCUMENT_VALUES: tuple[ReviewDocument, ...] = ("prenup", "setup", "disclosure", "worksheet",)

@dataclass(frozen=True, slots=True)
class EnumOption:
    id: str
    label: str

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["id"] = self.id
        out["label"] = self.label
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "EnumOption":
        return cls(
            id=data.get("id"),
            label=data.get("label"),
        )


@dataclass(frozen=True, slots=True)
class Question:
    id: str
    prompt: str
    answer_kind: str
    help: str | None = None
    enum_options: list[EnumOption] | None = None
    required: bool = True
    must_equal: Any | None = None
    """The walk fails unless the answer equals this (consent gates)."""
    ai_sanity: bool = False

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["id"] = self.id
        out["prompt"] = self.prompt
        if self.help is not None:
            out["help"] = self.help

        out["answer_kind"] = self.answer_kind
        if self.enum_options is not None:
            out["enum_options"] = [item.to_json() if item is not None else None for item in self.enum_options or []]

        out["required"] = self.required
        if self.must_equal is not None:
            out["must_equal"] = self.must_equal

        out["ai_sanity"] = self.ai_sanity
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "Question":
        return cls(
            id=data.get("id"),
            prompt=data.get("prompt"),
            answer_kind=data.get("answer_kind"),
            help=data.get("help"),
            enum_options=[EnumOption.from_json(item) for item in (data.get("enum_options") or [])],
            required=(data.get("required") if data.get("required") is not None else True),
            must_equal=data.get("must_equal"),
            ai_sanity=(data.get("ai_sanity") if data.get("ai_sanity") is not None else False),
        )


@dataclass(frozen=True, slots=True)
class Choice:
    id: str
    label: str
    next_node_id: str
    summary: str | None = None
    requires_entitlement: Entitlement | None = None
    skip_questions: bool = False

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["id"] = self.id
        out["label"] = self.label
        if self.summary is not None:
            out["summary"] = self.summary

        out["next_node_id"] = self.next_node_id
        if self.requires_entitlement is not None:
            out["requires_entitlement"] = self.requires_entitlement

        out["skip_questions"] = self.skip_questions
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "Choice":
        return cls(
            id=data.get("id"),
            label=data.get("label"),
            next_node_id=data.get("next_node_id"),
            summary=data.get("summary"),
            requires_entitlement=data.get("requires_entitlement"),
            skip_questions=(data.get("skip_questions") if data.get("skip_questions") is not None else False),
        )


@dataclass(frozen=True, slots=True)
class FormField:
    name: str
    help: str

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["name"] = self.name
        out["help"] = self.help
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "FormField":
        return cls(
            name=data.get("name"),
            help=data.get("help"),
        )


@dataclass(frozen=True, slots=True)
class FormGuide:
    id: str
    title: str
    official_url: str
    steps: list[str]
    jurisdiction: str | None = None
    fields: list[FormField] | None = None

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["id"] = self.id
        out["title"] = self.title
        out["official_url"] = self.official_url
        if self.jurisdiction is not None:
            out["jurisdiction"] = self.jurisdiction

        out["steps"] = self.steps
        if self.fields is not None:
            out["fields"] = [item.to_json() if item is not None else None for item in self.fields or []]

        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "FormGuide":
        return cls(
            id=data.get("id"),
            title=data.get("title"),
            official_url=data.get("official_url"),
            steps=(data.get("steps") or []),
            jurisdiction=data.get("jurisdiction"),
            fields=[FormField.from_json(item) for item in (data.get("fields") or [])],
        )


@dataclass(frozen=True, slots=True)
class DraftDocument:
    title: str
    markdown: str

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["title"] = self.title
        out["markdown"] = self.markdown
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "DraftDocument":
        return cls(
            title=data.get("title"),
            markdown=data.get("markdown"),
        )


@dataclass(frozen=True, slots=True)
class DocumentPack:
    schema_version: str
    binding: str
    disclaimer: str
    prenup_draft: DraftDocument
    setup_instructions: DraftDocument
    lawyer_comparison_worksheet: DraftDocument
    disclosure_schedule: DraftDocument
    """The user's own values, formatted as the exhibit counsel will ask for."""
    data_quality: list[str]
    """Gaps in the user's answers. Surfaced, never silently patched."""

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["schema_version"] = self.schema_version
        out["binding"] = self.binding
        out["disclaimer"] = self.disclaimer
        out["prenup_draft"] = self.prenup_draft.to_json() if self.prenup_draft is not None else None
        out["setup_instructions"] = self.setup_instructions.to_json() if self.setup_instructions is not None else None
        out["lawyer_comparison_worksheet"] = self.lawyer_comparison_worksheet.to_json() if self.lawyer_comparison_worksheet is not None else None
        out["disclosure_schedule"] = self.disclosure_schedule.to_json() if self.disclosure_schedule is not None else None
        out["data_quality"] = self.data_quality
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "DocumentPack":
        return cls(
            schema_version=data.get("schema_version"),
            binding=data.get("binding"),
            disclaimer=data.get("disclaimer"),
            prenup_draft=DraftDocument.from_json(data.get("prenup_draft")),
            setup_instructions=DraftDocument.from_json(data.get("setup_instructions")),
            lawyer_comparison_worksheet=DraftDocument.from_json(data.get("lawyer_comparison_worksheet")),
            disclosure_schedule=DraftDocument.from_json(data.get("disclosure_schedule")),
            data_quality=(data.get("data_quality") or []),
        )


@dataclass(frozen=True, slots=True)
class PathStateEnvelope:
    """The opto-sync record shape for a matter's path state."""

    scope: str
    record_id: str
    tree_id: str
    payload: Any

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["scope"] = self.scope
        out["record_id"] = self.record_id
        out["tree_id"] = self.tree_id
        out["payload"] = self.payload
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "PathStateEnvelope":
        return cls(
            scope=data.get("scope"),
            record_id=data.get("record_id"),
            tree_id=data.get("tree_id"),
            payload=data.get("payload"),
        )


@dataclass(frozen=True, slots=True)
class NodeView:
    """One step of the interview, as the API renders it."""

    matter_id: MatterId
    node_id: str
    kind: NodeKind
    title: str
    prompt: str
    not_legal_advice: bool
    questions: list[Question]
    choices: list[Choice]
    form_guides: list[FormGuide]
    history_len: int
    can_rewind: bool
    entitlement: Entitlement
    documents: DocumentPack | None = None
    sync: PathStateEnvelope | None = None

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["matter_id"] = self.matter_id
        out["node_id"] = self.node_id
        out["kind"] = self.kind
        out["title"] = self.title
        out["prompt"] = self.prompt
        out["not_legal_advice"] = self.not_legal_advice
        out["questions"] = [item.to_json() if item is not None else None for item in self.questions or []]
        out["choices"] = [item.to_json() if item is not None else None for item in self.choices or []]
        out["form_guides"] = [item.to_json() if item is not None else None for item in self.form_guides or []]
        out["history_len"] = self.history_len
        out["can_rewind"] = self.can_rewind
        out["entitlement"] = self.entitlement
        if self.documents is not None:
            out["documents"] = self.documents.to_json() if self.documents is not None else None

        if self.sync is not None:
            out["sync"] = self.sync.to_json() if self.sync is not None else None

        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "NodeView":
        return cls(
            matter_id=data.get("matter_id"),
            node_id=data.get("node_id"),
            kind=data.get("kind"),
            title=data.get("title"),
            prompt=data.get("prompt"),
            not_legal_advice=data.get("not_legal_advice"),
            questions=[Question.from_json(item) for item in (data.get("questions") or [])],
            choices=[Choice.from_json(item) for item in (data.get("choices") or [])],
            form_guides=[FormGuide.from_json(item) for item in (data.get("form_guides") or [])],
            history_len=data.get("history_len"),
            can_rewind=data.get("can_rewind"),
            entitlement=data.get("entitlement"),
            documents=(DocumentPack.from_json(data.get("documents")) if data.get("documents") is not None else None),
            sync=(PathStateEnvelope.from_json(data.get("sync")) if data.get("sync") is not None else None),
        )


@dataclass(frozen=True, slots=True)
class WalkBody:
    choice_id: str
    answers: dict[str, Any] | None = None
    confirm_override: bool = False
    """Proceed past a field-sanity rejection."""

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["choice_id"] = self.choice_id
        if self.answers is not None:
            out["answers"] = self.answers

        out["confirm_override"] = self.confirm_override
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "WalkBody":
        return cls(
            choice_id=data.get("choice_id"),
            answers=(data.get("answers") or {}),
            confirm_override=(data.get("confirm_override") if data.get("confirm_override") is not None else False),
        )


@dataclass(frozen=True, slots=True)
class FieldInput:
    question_id: str
    prompt: str
    value: str
    answer_kind: str = ""
    required: bool = True
    """Blank is a valid answer to an optional question. Defaults to required so an older client that omits the flag keeps the stricter behaviour."""

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["question_id"] = self.question_id
        out["prompt"] = self.prompt
        out["answer_kind"] = self.answer_kind
        out["value"] = self.value
        out["required"] = self.required
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "FieldInput":
        return cls(
            question_id=data.get("question_id"),
            prompt=data.get("prompt"),
            value=data.get("value"),
            answer_kind=(data.get("answer_kind") if data.get("answer_kind") is not None else ""),
            required=(data.get("required") if data.get("required") is not None else True),
        )


@dataclass(frozen=True, slots=True)
class FieldVerdict:
    question_id: str
    accepted: bool
    reason: str
    source: VerdictSource

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["question_id"] = self.question_id
        out["accepted"] = self.accepted
        out["reason"] = self.reason
        out["source"] = self.source
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "FieldVerdict":
        return cls(
            question_id=data.get("question_id"),
            accepted=data.get("accepted"),
            reason=data.get("reason"),
            source=data.get("source"),
        )


@dataclass(frozen=True, slots=True)
class ProviderVote:
    source: VerdictSource
    question_id: str
    accepted: bool
    reason: str
    error: str | None = None

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["source"] = self.source
        out["question_id"] = self.question_id
        out["accepted"] = self.accepted
        out["reason"] = self.reason
        if self.error is not None:
            out["error"] = self.error

        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "ProviderVote":
        return cls(
            source=data.get("source"),
            question_id=data.get("question_id"),
            accepted=data.get("accepted"),
            reason=data.get("reason"),
            error=data.get("error"),
        )


@dataclass(frozen=True, slots=True)
class SanityReport:
    accepted: bool
    override_allowed: bool
    """Always true: the user may confirm and submit anyway."""
    fields: list[FieldVerdict]
    provider_votes: list[ProviderVote]

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["accepted"] = self.accepted
        out["override_allowed"] = self.override_allowed
        out["fields"] = [item.to_json() if item is not None else None for item in self.fields or []]
        out["provider_votes"] = [item.to_json() if item is not None else None for item in self.provider_votes or []]
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "SanityReport":
        return cls(
            accepted=data.get("accepted"),
            override_allowed=data.get("override_allowed"),
            fields=[FieldVerdict.from_json(item) for item in (data.get("fields") or [])],
            provider_votes=[ProviderVote.from_json(item) for item in (data.get("provider_votes") or [])],
        )


@dataclass(frozen=True, slots=True)
class CheckFieldSanityRequest:
    fields: list[FieldInput]
    matter_id: str | None = None
    node_id: str | None = None

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        if self.matter_id is not None:
            out["matter_id"] = self.matter_id

        if self.node_id is not None:
            out["node_id"] = self.node_id

        out["fields"] = [item.to_json() if item is not None else None for item in self.fields or []]
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "CheckFieldSanityRequest":
        return cls(
            fields=[FieldInput.from_json(item) for item in (data.get("fields") or [])],
            matter_id=data.get("matter_id"),
            node_id=data.get("node_id"),
        )


@dataclass(frozen=True, slots=True)
class CheckFieldSanityResponse:
    report: SanityReport

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["report"] = self.report.to_json() if self.report is not None else None
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "CheckFieldSanityResponse":
        return cls(
            report=SanityReport.from_json(data.get("report")),
        )


@dataclass(frozen=True, slots=True)
class CounselAnswer:
    provider: CounselProvider
    markdown: str
    model: str
    error: str | None = None
    """Redacted provider failure; never carries a status code, key, or trace id. `markdown` is empty when set."""

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["provider"] = self.provider
        out["markdown"] = self.markdown
        if self.error is not None:
            out["error"] = self.error

        out["model"] = self.model
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "CounselAnswer":
        return cls(
            provider=data.get("provider"),
            markdown=data.get("markdown"),
            model=data.get("model"),
            error=data.get("error"),
        )


@dataclass(frozen=True, slots=True)
class CounselRoundTable:
    question: str
    answers: list[CounselAnswer]
    agreed_points: list[str]
    """Points more than one model raised, most-agreed first."""
    answered_by: int
    failed: list[str]
    disclaimer: str

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["question"] = self.question
        out["answers"] = [item.to_json() if item is not None else None for item in self.answers or []]
        out["agreed_points"] = self.agreed_points
        out["answered_by"] = self.answered_by
        out["failed"] = self.failed
        out["disclaimer"] = self.disclaimer
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "CounselRoundTable":
        return cls(
            question=data.get("question"),
            answers=[CounselAnswer.from_json(item) for item in (data.get("answers") or [])],
            agreed_points=(data.get("agreed_points") or []),
            answered_by=data.get("answered_by"),
            failed=(data.get("failed") or []),
            disclaimer=data.get("disclaimer"),
        )


@dataclass(frozen=True, slots=True)
class AskCounselRequest:
    matter_id: str
    question: str = ""
    scope: AskCounselScope | None = None
    document: ReviewDocument | None = None

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["matter_id"] = self.matter_id
        out["question"] = self.question
        if self.scope is not None:
            out["scope"] = self.scope

        if self.document is not None:
            out["document"] = self.document

        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "AskCounselRequest":
        return cls(
            matter_id=data.get("matter_id"),
            question=(data.get("question") if data.get("question") is not None else ""),
            scope=data.get("scope"),
            document=data.get("document"),
        )


@dataclass(frozen=True, slots=True)
class AskCounselResponse:
    round_table: CounselRoundTable
    providers_configured: list[CounselProvider]
    """Providers with keys on the server, so a client can explain an empty round table without guessing."""

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["round_table"] = self.round_table.to_json() if self.round_table is not None else None
        out["providers_configured"] = self.providers_configured
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "AskCounselResponse":
        return cls(
            round_table=CounselRoundTable.from_json(data.get("round_table")),
            providers_configured=(data.get("providers_configured") or []),
        )


@dataclass(frozen=True, slots=True)
class Avenue:
    id: str
    summary: str

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["id"] = self.id
        out["summary"] = self.summary
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "Avenue":
        return cls(
            id=data.get("id"),
            summary=data.get("summary"),
        )


@dataclass(frozen=True, slots=True)
class Place:
    id: str
    label: str
    aliases: list[str] | None = None

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["id"] = self.id
        out["label"] = self.label
        if self.aliases is not None:
            out["aliases"] = self.aliases

        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "Place":
        return cls(
            id=data.get("id"),
            label=data.get("label"),
            aliases=(data.get("aliases") or []),
        )


@dataclass(frozen=True, slots=True)
class GeographyCatalog:
    schema_version: str
    focus: str
    countries: list[Place]
    us_states: list[Place]

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["schema_version"] = self.schema_version
        out["focus"] = self.focus
        out["countries"] = [item.to_json() if item is not None else None for item in self.countries or []]
        out["us_states"] = [item.to_json() if item is not None else None for item in self.us_states or []]
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "GeographyCatalog":
        return cls(
            schema_version=data.get("schema_version"),
            focus=data.get("focus"),
            countries=[Place.from_json(item) for item in (data.get("countries") or [])],
            us_states=[Place.from_json(item) for item in (data.get("us_states") or [])],
        )


@dataclass(frozen=True, slots=True)
class ScheduleItem:
    index: int
    description: str
    approx_usd: float | None = None
    """Parsed from the same line when the user wrote an amount."""

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["index"] = self.index
        out["description"] = self.description
        if self.approx_usd is not None:
            out["approx_usd"] = self.approx_usd

        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "ScheduleItem":
        return cls(
            index=data.get("index"),
            description=data.get("description"),
            approx_usd=data.get("approx_usd"),
        )


@dataclass(frozen=True, slots=True)
class BalanceSheet:
    brokerage_usd: float | None = None
    crypto_usd: float | None = None
    home_equity_usd: float | None = None
    business_usd: float | None = None
    cash_usd: float | None = None
    retirement_usd: float | None = None
    other_assets_usd: float | None = None
    debts_usd: float | None = None

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        if self.brokerage_usd is not None:
            out["brokerage_usd"] = self.brokerage_usd

        if self.crypto_usd is not None:
            out["crypto_usd"] = self.crypto_usd

        if self.home_equity_usd is not None:
            out["home_equity_usd"] = self.home_equity_usd

        if self.business_usd is not None:
            out["business_usd"] = self.business_usd

        if self.cash_usd is not None:
            out["cash_usd"] = self.cash_usd

        if self.retirement_usd is not None:
            out["retirement_usd"] = self.retirement_usd

        if self.other_assets_usd is not None:
            out["other_assets_usd"] = self.other_assets_usd

        if self.debts_usd is not None:
            out["debts_usd"] = self.debts_usd

        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "BalanceSheet":
        return cls(
            brokerage_usd=data.get("brokerage_usd"),
            crypto_usd=data.get("crypto_usd"),
            home_equity_usd=data.get("home_equity_usd"),
            business_usd=data.get("business_usd"),
            cash_usd=data.get("cash_usd"),
            retirement_usd=data.get("retirement_usd"),
            other_assets_usd=data.get("other_assets_usd"),
            debts_usd=data.get("debts_usd"),
        )


@dataclass(frozen=True, slots=True)
class ExecutionTiming:
    wedding_date_raw: str
    seven_day_rule_applies: bool
    """True when a California-style seven-day presentation rule is in play."""
    steps: list[str]

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["wedding_date_raw"] = self.wedding_date_raw
        out["seven_day_rule_applies"] = self.seven_day_rule_applies
        out["steps"] = self.steps
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "ExecutionTiming":
        return cls(
            wedding_date_raw=data.get("wedding_date_raw"),
            seven_day_rule_applies=data.get("seven_day_rule_applies"),
            steps=(data.get("steps") or []),
        )


@dataclass(frozen=True, slots=True)
class MatterFacts:
    party_a: str
    party_b: str
    countries: str
    us_states: str
    ceremony_place: str
    likely_domicile: str
    governing_state: str
    wedding_date: str
    citizenships: str
    real_estate_locations: str
    balance_sheet: BalanceSheet
    schedule_a: list[ScheduleItem]
    lineage_clause: bool
    both_counsel: bool
    disclosed: bool
    support_addressed: bool
    has_children: bool
    prior_marriage: bool
    business_active_role: bool
    statements_saved: bool
    joint_accounts: bool
    want_scheduled_trust: bool
    trust_name: str
    child_count: str
    payout_start_age: str
    monthly_payout_usd: str
    independent_trustee: bool
    settlor_not_payee: bool
    timing: ExecutionTiming
    data_quality: list[str]
    """Gaps a lawyer will notice immediately. Shown to the user, not hidden."""

    def to_json(self) -> dict[str, Any]:
        out: dict[str, Any] = {}
        out["party_a"] = self.party_a
        out["party_b"] = self.party_b
        out["countries"] = self.countries
        out["us_states"] = self.us_states
        out["ceremony_place"] = self.ceremony_place
        out["likely_domicile"] = self.likely_domicile
        out["governing_state"] = self.governing_state
        out["wedding_date"] = self.wedding_date
        out["citizenships"] = self.citizenships
        out["real_estate_locations"] = self.real_estate_locations
        out["balance_sheet"] = self.balance_sheet.to_json() if self.balance_sheet is not None else None
        out["schedule_a"] = [item.to_json() if item is not None else None for item in self.schedule_a or []]
        out["lineage_clause"] = self.lineage_clause
        out["both_counsel"] = self.both_counsel
        out["disclosed"] = self.disclosed
        out["support_addressed"] = self.support_addressed
        out["has_children"] = self.has_children
        out["prior_marriage"] = self.prior_marriage
        out["business_active_role"] = self.business_active_role
        out["statements_saved"] = self.statements_saved
        out["joint_accounts"] = self.joint_accounts
        out["want_scheduled_trust"] = self.want_scheduled_trust
        out["trust_name"] = self.trust_name
        out["child_count"] = self.child_count
        out["payout_start_age"] = self.payout_start_age
        out["monthly_payout_usd"] = self.monthly_payout_usd
        out["independent_trustee"] = self.independent_trustee
        out["settlor_not_payee"] = self.settlor_not_payee
        out["timing"] = self.timing.to_json() if self.timing is not None else None
        out["data_quality"] = self.data_quality
        return out

    @classmethod
    def from_json(cls, data: dict[str, Any]) -> "MatterFacts":
        return cls(
            party_a=data.get("party_a"),
            party_b=data.get("party_b"),
            countries=data.get("countries"),
            us_states=data.get("us_states"),
            ceremony_place=data.get("ceremony_place"),
            likely_domicile=data.get("likely_domicile"),
            governing_state=data.get("governing_state"),
            wedding_date=data.get("wedding_date"),
            citizenships=data.get("citizenships"),
            real_estate_locations=data.get("real_estate_locations"),
            balance_sheet=BalanceSheet.from_json(data.get("balance_sheet")),
            schedule_a=[ScheduleItem.from_json(item) for item in (data.get("schedule_a") or [])],
            lineage_clause=data.get("lineage_clause"),
            both_counsel=data.get("both_counsel"),
            disclosed=data.get("disclosed"),
            support_addressed=data.get("support_addressed"),
            has_children=data.get("has_children"),
            prior_marriage=data.get("prior_marriage"),
            business_active_role=data.get("business_active_role"),
            statements_saved=data.get("statements_saved"),
            joint_accounts=data.get("joint_accounts"),
            want_scheduled_trust=data.get("want_scheduled_trust"),
            trust_name=data.get("trust_name"),
            child_count=data.get("child_count"),
            payout_start_age=data.get("payout_start_age"),
            monthly_payout_usd=data.get("monthly_payout_usd"),
            independent_trustee=data.get("independent_trustee"),
            settlor_not_payee=data.get("settlor_not_payee"),
            timing=ExecutionTiming.from_json(data.get("timing")),
            data_quality=(data.get("data_quality") or []),
        )


# ------------------------------------------------------------- transport

Delivery = Literal["direct", "opto_sync_queued"]
RecordIdFrom = Literal["path", "request", "minted"]

@dataclass(frozen=True, slots=True)
class OptoSyncBinding:
    """Static description of a queued call's opto-sync record."""

    table: str
    operation: str
    record_id_from: RecordIdFrom
    record_id_name: str | None = None

@dataclass(frozen=True, slots=True)
class RpcRequest:
    """One outbound call, fully resolved. Transports never re-derive paths."""

    key: str
    method: str
    path: str
    #: The unsubstituted template, so a transport can locate a named
    #: parameter inside `path` instead of guessing at its position.
    path_template: str = ""
    query: tuple[tuple[str, str], ...] = ()
    body: str | None = None
    delivery: Delivery = "direct"
    opto_sync: OptoSyncBinding | None = None

class RpcTransport(Protocol):
    """The single seam between generated code and the network.

        Implement it over your HTTP client for direct calls and over an
        opto-sync queue for queued ones. The dependency runs one way: this
        module calls opto-sync, never the reverse.
        """

    def call(self, request: RpcRequest) -> str: ...

def encode_segment(value: str) -> str:
    """Escape one path segment so an id containing `/` cannot reshape the URL."""
    from urllib.parse import quote

    return quote(str(value), safe="")


def _query_value(value: Any) -> str:
    if isinstance(value, bool):
        return "true" if value else "false"
    return value if isinstance(value, str) else str(value)

def healthz_path() -> str:
    """`GET /healthz`"""
    return "/healthz"

def create_matter_path() -> str:
    """`POST /v1/matters`"""
    return "/v1/matters"

def get_matter_path(id: MatterId) -> str:
    """`GET /v1/matters/{id}`"""
    return "/v1/matters/" + encode_segment(_query_value(id))

def walk_matter_path(id: MatterId) -> str:
    """`POST /v1/matters/{id}/walk`"""
    return "/v1/matters/" + encode_segment(_query_value(id)) + "/walk"

def get_documents_path(id: MatterId) -> str:
    """`GET /v1/matters/{id}/documents`"""
    return "/v1/matters/" + encode_segment(_query_value(id)) + "/documents"

def get_facts_path(id: MatterId) -> str:
    """`GET /v1/matters/{id}/facts`"""
    return "/v1/matters/" + encode_segment(_query_value(id)) + "/facts"

def avenues_path() -> str:
    """`GET /v1/avenues`"""
    return "/v1/avenues"

def geography_path() -> str:
    """`GET /v1/geography`"""
    return "/v1/geography"

def check_field_sanity_path() -> str:
    """`POST /pmap.v1.Interview/CheckFieldSanity`"""
    return "/pmap.v1.Interview/CheckFieldSanity"

def ask_counsel_path() -> str:
    """`POST /pmap.v1.Interview/AskCounsel`"""
    return "/pmap.v1.Interview/AskCounsel"

def check_field_sanity_rest_path() -> str:
    """`POST /v1/fields/sanity`"""
    return "/v1/fields/sanity"

def healthz(transport: RpcTransport) -> str:
    """healthz"""
    path = healthz_path()
    pairs: tuple[tuple[str, str], ...] = ()
    raw = transport.call(
        RpcRequest(
            key="healthz",
            method="GET",
            path=path,
            path_template="/healthz",
            query=pairs,
            body=None,
            delivery="direct",
            opto_sync=None,
        )
    )
    return json.loads(raw)  # type: ignore[no-any-return]

def create_matter(transport: RpcTransport) -> NodeView:
    """Start a free matter."""
    path = create_matter_path()
    pairs: tuple[tuple[str, str], ...] = ()
    raw = transport.call(
        RpcRequest(
            key="create_matter",
            method="POST",
            path=path,
            path_template="/v1/matters",
            query=pairs,
            body=None,
            delivery="direct",
            opto_sync=None,
        )
    )
    return NodeView.from_json(json.loads(raw))

def get_matter(transport: RpcTransport, id: MatterId) -> NodeView:
    """get_matter"""
    path = get_matter_path(id)
    pairs: tuple[tuple[str, str], ...] = ()
    raw = transport.call(
        RpcRequest(
            key="get_matter",
            method="GET",
            path=path,
            path_template="/v1/matters/{id}",
            query=pairs,
            body=None,
            delivery="direct",
            opto_sync=None,
        )
    )
    return NodeView.from_json(json.loads(raw))

def walk_matter(transport: RpcTransport, id: MatterId, body: WalkBody) -> NodeView:
    """Advance the interview by one choice."""
    path = walk_matter_path(id)
    pairs: tuple[tuple[str, str], ...] = ()
    payload = json.dumps(body.to_json())
    raw = transport.call(
        RpcRequest(
            key="walk_matter",
            method="POST",
            path=path,
            path_template="/v1/matters/{id}/walk",
            query=pairs,
            body=payload,
            delivery="opto_sync_queued",
            opto_sync=OptoSyncBinding(table="pmap_path_state", operation="upsert", record_id_from="path", record_id_name="id"),
        )
    )
    return NodeView.from_json(json.loads(raw))

def get_documents(transport: RpcTransport, id: MatterId) -> DocumentPack:
    """get_documents"""
    path = get_documents_path(id)
    pairs: tuple[tuple[str, str], ...] = ()
    raw = transport.call(
        RpcRequest(
            key="get_documents",
            method="GET",
            path=path,
            path_template="/v1/matters/{id}/documents",
            query=pairs,
            body=None,
            delivery="direct",
            opto_sync=None,
        )
    )
    return DocumentPack.from_json(json.loads(raw))

def get_facts(transport: RpcTransport, id: MatterId) -> MatterFacts:
    """get_facts"""
    path = get_facts_path(id)
    pairs: tuple[tuple[str, str], ...] = ()
    raw = transport.call(
        RpcRequest(
            key="get_facts",
            method="GET",
            path=path,
            path_template="/v1/matters/{id}/facts",
            query=pairs,
            body=None,
            delivery="direct",
            opto_sync=None,
        )
    )
    return MatterFacts.from_json(json.loads(raw))

def avenues(transport: RpcTransport) -> list[Avenue]:
    """avenues"""
    path = avenues_path()
    pairs: tuple[tuple[str, str], ...] = ()
    raw = transport.call(
        RpcRequest(
            key="avenues",
            method="GET",
            path=path,
            path_template="/v1/avenues",
            query=pairs,
            body=None,
            delivery="direct",
            opto_sync=None,
        )
    )
    return json.loads(raw)  # type: ignore[no-any-return]

def geography(transport: RpcTransport) -> GeographyCatalog:
    """geography"""
    path = geography_path()
    pairs: tuple[tuple[str, str], ...] = ()
    raw = transport.call(
        RpcRequest(
            key="geography",
            method="GET",
            path=path,
            path_template="/v1/geography",
            query=pairs,
            body=None,
            delivery="direct",
            opto_sync=None,
        )
    )
    return GeographyCatalog.from_json(json.loads(raw))

def check_field_sanity(transport: RpcTransport, body: CheckFieldSanityRequest) -> CheckFieldSanityResponse:
    """Connect JSON unary (not gRPC)."""
    path = check_field_sanity_path()
    pairs: tuple[tuple[str, str], ...] = ()
    payload = json.dumps(body.to_json())
    raw = transport.call(
        RpcRequest(
            key="CheckFieldSanity",
            method="POST",
            path=path,
            path_template="/pmap.v1.Interview/CheckFieldSanity",
            query=pairs,
            body=payload,
            delivery="direct",
            opto_sync=None,
        )
    )
    return CheckFieldSanityResponse.from_json(json.loads(raw))

def ask_counsel(transport: RpcTransport, body: AskCounselRequest) -> AskCounselResponse:
    """Ask every configured model about one matter."""
    path = ask_counsel_path()
    pairs: tuple[tuple[str, str], ...] = ()
    payload = json.dumps(body.to_json())
    raw = transport.call(
        RpcRequest(
            key="AskCounsel",
            method="POST",
            path=path,
            path_template="/pmap.v1.Interview/AskCounsel",
            query=pairs,
            body=payload,
            delivery="direct",
            opto_sync=None,
        )
    )
    return AskCounselResponse.from_json(json.loads(raw))

def check_field_sanity_rest(transport: RpcTransport, body: CheckFieldSanityRequest) -> CheckFieldSanityResponse:
    """REST alias of CheckFieldSanity."""
    path = check_field_sanity_rest_path()
    pairs: tuple[tuple[str, str], ...] = ()
    payload = json.dumps(body.to_json())
    raw = transport.call(
        RpcRequest(
            key="check_field_sanity_rest",
            method="POST",
            path=path,
            path_template="/v1/fields/sanity",
            query=pairs,
            body=payload,
            delivery="direct",
            opto_sync=None,
        )
    )
    return CheckFieldSanityResponse.from_json(json.loads(raw))

@dataclass(frozen=True, slots=True)
class OperationInfo:
    """One row per declared operation."""

    key: str
    path: str
    methods: tuple[str, ...]
    delivery: Delivery

OPERATIONS: tuple[OperationInfo, ...] = (
    OperationInfo(key="healthz", path="/healthz", methods=("GET",), delivery="direct"),
    OperationInfo(key="create_matter", path="/v1/matters", methods=("POST",), delivery="direct"),
    OperationInfo(key="get_matter", path="/v1/matters/{id}", methods=("GET",), delivery="direct"),
    OperationInfo(key="walk_matter", path="/v1/matters/{id}/walk", methods=("POST",), delivery="opto_sync_queued"),
    OperationInfo(key="get_documents", path="/v1/matters/{id}/documents", methods=("GET",), delivery="direct"),
    OperationInfo(key="get_facts", path="/v1/matters/{id}/facts", methods=("GET",), delivery="direct"),
    OperationInfo(key="avenues", path="/v1/avenues", methods=("GET",), delivery="direct"),
    OperationInfo(key="geography", path="/v1/geography", methods=("GET",), delivery="direct"),
    OperationInfo(key="CheckFieldSanity", path="/pmap.v1.Interview/CheckFieldSanity", methods=("POST",), delivery="direct"),
    OperationInfo(key="AskCounsel", path="/pmap.v1.Interview/AskCounsel", methods=("POST",), delivery="direct"),
    OperationInfo(key="check_field_sanity_rest", path="/v1/fields/sanity", methods=("POST",), delivery="direct"),
)

#: Operations that route through opto-sync's durable queue.
QUEUED_OPERATIONS: tuple[str, ...] = ("walk_matter",)
