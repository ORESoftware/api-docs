# Server-stream ABI integration

This branch wires the provider-neutral server-stream operation ABI through the canonical Rust operation macro and runtime. The file intentionally records the integration trigger and may remain as branch evidence.
