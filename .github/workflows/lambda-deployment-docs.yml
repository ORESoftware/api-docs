name: lambda-deployment-docs

on:
  pull_request:
    paths:
      - "contracts/lambda-deployment-docs/**"
      - "contracts/tjsv-consumer.lock.json"
      - ".github/workflows/lambda-deployment-docs.yml"
  push:
    branches: [main]
    paths:
      - "contracts/lambda-deployment-docs/**"
      - "contracts/tjsv-consumer.lock.json"
      - ".github/workflows/lambda-deployment-docs.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: lambda-deployment-docs-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  peer-authority-parity:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Check out exact candidate
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
          persist-credentials: false
          show-progress: false
          fetch-depth: 1

      - name: Verify exact candidate revision
        env:
          EXPECTED_SHA: ${{ github.event.pull_request.head.sha || github.sha }}
        run: test "$(git rev-parse HEAD)" = "$EXPECTED_SHA"

      - name: Require complete zero-waiver declaration mapping
        run: |
          set -euo pipefail
          jq -e '
            .schema == "ores.typespec-json-schema-validator.mapping/v1" and
            (.declarations | length) == 7 and
            .ignore.typespec == [] and
            .ignore.generated == [] and
            .ignore.authored == [] and
            ([.declarations[].typespec] | unique | length) == 7 and
            ([.declarations[].generated] | unique | length) == 7 and
            ([.declarations[].authored] | unique | length) == 7
          ' contracts/lambda-deployment-docs/tjsv.mapping.json

      - name: Compare independent TypeSpec and JSON Schema authorities
        uses: ORESoftware/typespec-json-schema-validator@7cf36bbcbd9523caaf894ac9188bd29633b7ac9f
        with:
          typespec: contracts/lambda-deployment-docs/typespec/main.tsp
          schema: contracts/lambda-deployment-docs/json-schema/lambda-deployment-docs.schema.json
          mapping: contracts/lambda-deployment-docs/tjsv.mapping.json
          report: .typespec-json-schema-validator/lambda-deployment-docs/report.json
          sarif: .typespec-json-schema-validator/lambda-deployment-docs/report.sarif
          contract_ir: .typespec-json-schema-validator/lambda-deployment-docs/contract-ir.json
          output_dir: .typespec-json-schema-validator/lambda-deployment-docs/generated
          int64_strategy: number
          quiet: "false"

      - name: Require peer authorities and non-authoritative derived evidence
        run: |
          set -euo pipefail
          jq -e '
            .schema == "ores.typespec-json-schema-validator.report/v1" and
            .status == "passed" and
            .zeroUnexplainedFindings == true and
            .authorities.precedence == "none" and
            .authorities.typespec.authority == "independently-authored" and
            .authorities.jsonSchema.authority == "independently-authored" and
            .authorities.typespec.generatedJsonSchemaRole == "comparison-evidence-only" and
            (.findings | length) == 0 and
            (.differential.summary.probesEvaluated // 0) > 0 and
            (.differential.summary.divergences // 0) == 0 and
            (.differential.summary.refusals // 0) == 0
          ' .typespec-json-schema-validator/lambda-deployment-docs/report.json
          jq -e '
            .schema == "ores.typespec-json-schema-validator.contract-ir/v1" and
            .status == "passed" and
            .admissible == true and
            .role == "downstream-derived-parity-artifact" and
            .editableAuthority == false
          ' .typespec-json-schema-validator/lambda-deployment-docs/contract-ir.json

      - name: Prove authored authorities were not rewritten
        run: git diff --exit-code -- contracts/lambda-deployment-docs

      - name: Retain deterministic TJSV evidence
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: lambda-deployment-docs-${{ github.event.pull_request.head.sha || github.sha }}
          path: .typespec-json-schema-validator/lambda-deployment-docs
          include-hidden-files: true
          if-no-files-found: warn
          retention-days: 30
