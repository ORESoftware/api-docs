name: graphql-rpc-projection

on:
  pull_request:
    paths:
      - 'macros/graphql-rust/**'
      - 'macros/rpc-rust/**'
      - 'contracts/graphql-projection/**'
      - 'docs/graphql-projection.md'
      - 'Cargo.toml'
  push:
    branches: [main]
    paths:
      - 'macros/graphql-rust/**'
      - 'macros/rpc-rust/**'
      - 'contracts/graphql-projection/**'
      - 'docs/graphql-projection.md'
      - 'Cargo.toml'

permissions:
  contents: read

jobs:
  graphql-macro:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test --manifest-path macros/graphql-rust/Cargo.toml

  rpc-publication-macros:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - run: cargo test --manifest-path macros/rpc-rust/Cargo.toml

  contract-shape:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node -e 'const fs=require("fs"); const s=JSON.parse(fs.readFileSync("contracts/graphql-projection/graphql-projection.schema.json","utf8")); if(s.properties.endpoint.const!=="/v1/graphql") process.exit(1); if(s.properties.authority.const!=="funcs.rs") process.exit(1); const p=s.$defs.operation.properties; if(!p.semantic_source||!p.semantic_authority) process.exit(1);'
