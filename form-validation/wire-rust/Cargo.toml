[package]
name = "ores-form-validation-wire"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"
publish = false
description = "TJSV-checked value-free form validation message codecs"
repository = "https://github.com/ORESoftware/api-docs"

[workspace]

[dependencies]
ores-form-validation = { path = "../rust" }
serde = { version = "=1.0.229", features = ["derive"] }

[dev-dependencies]
serde_json = "=1.0.151"

[lints.rust]
unsafe_code = "forbid"
