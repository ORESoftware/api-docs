// Authored top-level TypeSpec authority — never generated from JSON Schema.
// Independently maintained and converged with the peer JSON Schema/OpenAPI lane.
// An unexplained mismatch pauses the release; neither authority overwrites the other.

import "./v1.tsp";
import "./v2.tsp";
import "./telemetry.tsp";
