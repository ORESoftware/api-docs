// Authored P0 — do not generate this from json-schema/.
// Independently maintained against json-schema/rpc-*.schema.json.
// A mismatch is a release veto, not a TypeSpec overwrite.

import "./v1.tsp";
import "./v2.tsp";
import "./telemetry.tsp";
