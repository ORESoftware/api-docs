name: ores_ridl_runtime
description: Browser, Flutter, and server-safe RIDL frame codec for WebSocket and TCP.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.6.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
